use super::registers::Registers;
use super::registers::Flags;

pub struct Instructions;

impl Instructions {
    //Todo: implement the instruction set, will later be inhereted by the CPU for opcode matching
    // But we can put all the raw functionality here
}